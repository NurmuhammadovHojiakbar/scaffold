import HeaderInfo from "./header-info";
import "./header.scss"

const Header = () => {
    return (
        <header className="site-header">
            <HeaderInfo />
        </header>
    );
}
 
export default Header;